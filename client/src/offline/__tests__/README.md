# 🧪 اختبارات نظام المزامنة (Sync Engine)

## الهيكل المقترح للاختبارات

1. **sync.test.ts**:
   - اختبار retry logic (5 محاولات).
   - اختبار المزامنة في حالة وجود اتصال/عدم وجود اتصال.
   - اختبار المزامنة المجمعة (Batch Sync).

2. **db.test.ts**:
   - اختبار القراءة والكتابة في IndexedDB.
   - اختبار تنظيف البيانات (Data Cleanup).
   - اختبار استرجاع النسخ الاحتياطية.

3. **conflict.test.ts**:
   - اختبار استراتيجيات حل التضارعات الأربعة:
     - الخادم يربح (Server Wins).
     - العميل يربح (Client Wins).
     - الأحدث يربح (Latest Wins).
     - الدمج اليدوي (Manual Merge).
