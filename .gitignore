node_modules
dist
project.tar.gz
*.log
.git
.replit
replit.nix
attached_assets
backups
uploads/
.cache
.config
.local
.upm
*.tar.gz
*.zip
*.rar
update_and_test.py
www/
client/www/
output_apks/
.npmrc

# بيانات البيئة — لا تُتتبع أبداً
.env
.env.*
!.env.example

# مفاتيح وأسرار
SERVICE_ACCOUNT_KEY.json
google-services.json
*.pem
*.key

# قواعد بيانات محلية
*.db
*.sqlite
*.sqlite3

# بيانات واتساب المستوردة (PII)
wa-import-data/

# جلسة واتساب
auth_info_baileys/

# Android signing keys (NEVER commit)
*.keystore
*.jks
android/app/axion-release.keystore
administrator/.axion-keystore/

# Firebase service accounts and config
google-services.json
firebase-service-account*.json

# snapshot المتغيرات (يحتوي أسرار، لا يُرفع لـ Git)
.env.snapshot
.env.before-restore-*
.transfer-tmp/
.transfer-backup-*/
